# atproto read spike — notes and verdict record

Plan: `docs/plans/2026-08-05-001-feat-content-delivery-decoupling-plan.md`, unit U5.
Time-box: 3 working days from live-phase start. Overrun = fail (KTD8).
Started: _(set when the live phase starts)_

## Configuration under test

- Repo: _(handle/DID of the test account)_
- PDS: `bsky.social` entryway → _(resolved PDS host)_
- Collection: `social.dyad.unfolding.entry` (unregistered custom NSID — settled; no AppView indexes it and the same codebase writes and reads the shape)
- Source gate: `CONTENT_SOURCE=atproto`, `CONTENT_ATPROTO_REPO=<handle>` on the **preview** environment only
- rkey = slug

## Exit criteria (KTD8 — all eight must hold; any miss is a fail)

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | Renders on a real Cloudflare preview deployment | pending | |
| 2 | No `@prefig/upact-atproto` / `@atproto/api` import; zero new runtime deps | pending | `git grep` + package.json diff |
| 3 | Clean build + cold start under `nodejs_compat` (tail first request of a fresh isolate) | pending | |
| 4 | Publish-to-visible p95 ≤ 5 min over ≥ 20 scripted cycles (`spike-atproto.mjs measure`); state which path was measured (KV invalidation, cron-poll, or both) | pending | |
| 5 | Simulated PDS outage serves last-known-good | pending | |
| 6 | One blob mirrored into Supabase Storage with correct content type (`spike-atproto.mjs upload-blob` + `mirror`) | pending | |
| 7 | PDS rate limit documented/measured ≥ 10× the poll rate implied by the chosen TTL (bsky.social documents 3000 req / 5 min / IP; applies to the PDS actually used) | pending | |
| 8 | The essay author publishes one edition end-to-end without a developer present, through a tool that exists today; record what the tool was. If production authoring additionally needs a purpose-built editor, cost it here | pending | |

## Test-scenario deviations

- The `visibility` exclusion scenario is skipped: it is defined by the WhiteWind lexicon, and this spike uses a custom NSID that has no `visibility` field (noted per plan U5).

## Measurements

_(publish-to-visible cycles, TTFB, rate-limit observations)_

## Findings

_(what worked, what broke, workerd behaviour, anything the plan's assumptions got wrong)_

## Verdict

_(pass/fail per criterion; overall atproto-based or not — written either way, weighed against KTD9's rehearsal case)_
